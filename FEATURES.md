# WBS · Gantt 功能需求与现状报告

> 本报告整合了开发过程中提出的所有较为明确的功能需求（需求原文按中文原样摘录），以及当前版本（v0.3.0，待发布）的最终实现情况。用于记录「要做什么」与「已经做成什么样」。

---

## 一、项目概述

WBS · Gantt 是一个基于 **WBS（工作分解结构）+ 甘特图** 的任务进度管理工具。用户将任务按父子关系分类填入，工具围绕甘特图提供直观、可视化的进度管理服务。

- **前端**：React 18 · TypeScript · Vite · Zustand · Tailwind CSS。依赖表刻意保持精简——语言与主题都是自己实现的（`src/lib/i18n.ts`、`src/lib/theme.ts`），没有引入 i18n 或主题库。
- **桌面端**：Tauri 2（Rust）
- **部署**：Web 版 Cloudflare Workers 静态资源；桌面版 Windows 安装包（NSIS）

---

## 二、数据模型

```ts
Project { id, name, color, description }

Task {
  id, name, description, parentId, projectId, type,        // type: 'phase' | 'long-term'
  isTodo,                                                   // 待办：无日期、无优先级、无进度
  startDate, endDate,                                       // yyyy-MM-dd；长期任务与待办为 null
  strictProgress,                                           // 严格进度计算开关（仅 phase 有效）
  paused, pauseDate, pauses[],                              // 暂停/恢复
  priority,                                                 // 待办为 null
  tags, dependencies, createdAt, updatedAt
}

TaskLog { id, taskId, date, content, targetProgress?, createdAt, updatedAt }
// content 为原始文本：换行 = 新条目，行首 Tab = 子条目
// targetProgress 为「当日结束时到达的进度」绝对值（0–100），非增量；仅严格任务使用
```

- `progress`、`status` 均为**派生值**（不存储，实时计算）。
- `isTodo` 是**权威字段**：带它的任务不会被推导出任何日期、进度或汇总状态。

---

## 三、功能清单（当前实现）

### 1. WBS 任务树
- 多层级父子结构，自动 WBS 编号（`1` / `1.1` / `1.1.1`），可展开/折叠。
- 按层级缩进（每级 16px）。
- 三级排版差异化：
  - 行高：一级 48px / 二级 36px / 三级 32px（一级→二级差 12px，二级→三级差 4px）。
  - 任务条厚度：40 / 20 / 12px。
  - 名称字号/字重：17px 粗体 / 13px 中等 / 12px 常规。
  - 第四级及更深沿用第三级。

### 2. 甘特图
- 连续画布，两层重叠：日期网格为全宽底层；左侧任务标签为半透明（30%）毛玻璃顶层。
- 左侧面板宽 560px，可拖动分割条（480–960px）。
- 任务条：按住中间拖动 = 移动；按住左右两端拖动 = 调整起止日期。
- 长期目标：整条渐变色（向右淡出，末端 30% 透明度）。
- 「今天」黄线标记。

### 3. 时间轴
- 五种粒度：Day / Week / Month / Quarter / Year（列宽 40 / 56 / 90 / 80 / 200px）。
- 默认视图为 **day**。
- **固定范围**（v0.2.2 起）：最早到今天前 30 天，最晚到「最后一个已排期（非长期）任务的截止日 + 10 天」，且不少于今天前后各 30 天。范围**跟随左上角的项目筛选**变化——切换项目时任务条位置完全不动，只有轴的右端长度变（`dateToX` 只依赖起点、列宽和粒度）。
- **粗粒度的对称下限**：Day 之外的单位，下限取「今天 ±（铺满画布所需列数 ÷ 2 向上取整，再 +5）个单位」。不加这条的话，80px 宽的季度列会把 130 天缩成两个格子、轴塌成左边一小条。另保证总跨度不窄于画布（保护超宽屏下的 day 粒度）。
- 改为**横向滚动浏览**，已移除上一页 / 下一页按钮；保留 **Back to Today** 滚回今天。今天的日期默认停在任务栏右缘往右 160px 处。
- 工具栏显示当前覆盖的日期范围（如 `Aug 13 – Dec 21`），取代原来的「当前周期」标签。

### 4. 进度计算（派生）
- **非严格 phase**：`已过天数 / 总天数`（自动随日期增长，起始为 0，走完为 100%）。
- **严格 phase**：两条路，取决于**最新一条日志**填没填「目标进度」——
  - 填了 → 直接采用该值（绝对值，非增量；是覆盖而非取最大，后写的小值会让进度回落）。
  - 没填 → 自动累计：`已写日志的天数 / 总天数`。
  - 一条日志都没有 → 0%，**不随时间增长**。
- **长期任务**：无进度（`null`，界面不显示）。
- **待办**：无进度（`null`）。
- **父任务**：子任务进度的平均值（忽略无进度的子任务）。
- 暂停期间：非严格任务进度**冻结在暂停日**。
- 新建任务进度默认 0。

### 5. 状态计算（派生）
优先级从高到低：
1. 待办 → `todo`
2. 暂停中 → `paused`
3. 进度 ≥ 100% → `completed`
4. 今天 < 起始日期 → `not-started`
5. 严格任务 且 今天 > 终止日期 → `delayed`
6. 其余 → `in-progress`

- 共六种状态：待办 / 未开始 / 进行中 / 已完成 / 暂停 / 逾期。
- 非严格任务**没有逾期态**（日期走完即自动完成）。
- 严格任务进度未满且已过 endDate 才显示 `delayed`。
- 父任务状态 = 子任务状态的聚合；待办父任务不从子任务继承状态。

### 6. 待办（To-do）
- 一种「还没想好什么时候做」的任务形态：**无日期、无优先级、无进度**，也不参与父任务的进度与状态汇总。
- 甘特图左栏以虚线框 + 专属紫色标识，与已排期任务一眼可分。
- **启动待办**：转成有日程的任务时，一次性填好起止日期与优先级。
- **待办文件夹**：同一父任务下的待办在左栏合成一个可折叠分组，支持勾选、批量恢复成任务、批量删除。
- 待办不入日历（无日期），也不计入任何严格日志覆盖率。

### 7. 任务日志
- **编写入口**（仅 in-progress / delayed 任务）：
  - 悬停图标区「写日志」按钮；
  - 任务详情页「Write log」按钮；
  - 右键菜单「Write log」；
  - **日期详情面板**中按「为这一天补写日志」（带入该日日期）。
- **日志编辑器**：日期（默认今天，可改）+ 内容 textarea +（仅严格 phase）默认折叠的「目标进度」项。
- **文本格式**：换行 = 新条目（自动加 `·`）；换行 + Tab = 子条目（`·` 跟随缩进）。编辑器内 Tab 键用于缩进。
- **历史查看**：任务详情页 History 折叠列表（可编辑/删除单条日志）。
- **日志页**（左侧导航 Gantt 下方）：默认按日分组铺成便利贴墙，每贴预览当天的任务与条目（最多 2 个任务 × 2 条），支持 project → 一级 → 二级… 的树形筛选；点开单日可统一查看/编辑/删除。

### 8. 暂停 / 恢复
- 暂停：任务条变灰、右端在暂停日截断；记录剩余天数 n。
- 恢复：结束日期 = 恢复日 + n（即顺延暂停时长）；颜色恢复。
- 甘特图：暂停中为单段灰条；恢复后为**两端（或多段）日期条**，暂停期留空。
- 详情页：
  - 暂停中：显示「Paused. Started… · paused on… · n days elapsed」+「Resume」按钮。
  - 恢复后：结束日期显示为「End (postponed)」；Start/End 之间可折叠展示暂停/恢复历史（可多次）。

### 9. 详情面板
- **任务详情**：WBS、名称、类型、进度模式（Strict/Auto）、状态、优先级、起止日期、进度条、标签、依赖、子任务、描述、写日志、历史日志、暂停/恢复。
- **项目详情**：进度、任务统计、描述、跳转甘特图。
- **日期详情**（v0.2.2）：点甘特图时间轴表头或日历的任意一天打开。当天排期的任务分「严格任务」「其他任务」两组，逐条显示进度，并标注 `No log` / `Overdue` / `Paused`；可就地补写日志，底部按钮一次读完当天全部日志。**未来日**显示 `Not yet due`，行上不出现 `No log` 徽章（见第 10 节）。

### 10. 日历
- 月历 6×7，周一起始，可翻月、可跳回今天；点格子打开日期详情面板，点任务名打开任务详情，支持键盘（Enter / Space）。
- **每格只列当天开始（▶）和当天截止（⏹）的叶子任务**，截止优先。不列"当天在进行的全部任务"——那是甘特图的主场；日历回答的是它独有的问题：今天有什么开始、有什么到期。只列叶子任务，父任务的起止由子任务推导，父子同列会白占一行。
- **右上角义务方块**：每个严格日志义务一个方块，写了填实（绿），没写留空框——一条都没写时描边为红，写了一部分为琥珀。超过 6 个义务退回显示 `3/7` 这样的数字。**刻意用离散表达**：每天的义务数通常只有一两个，任何比例式的画法（高度、宽度、进度条）都只能画出 0% / 50% / 100% 三个台阶，等于声称了数据不具备的分辨率。
- **任务名的颜色只有一个含义：严格任务用黄色。** 写了日志的任务名加删除线。两者合起来可区分「本该写、写了」「本该写、欠着」「非严格任务上的随手记录」。**不再使用状态色**——状态是甘特图的事，日历里再用一遍会和方块的信息打架。
- **无义务且已过去的日子**：绿色细描边，不加辉光。**接近完成或写满**（≥90%）：轻微辉光。**未来日**：不做任何标记。
- 不跟随甘特图的项目筛选，定位为全局总览。

### 11. Manage
（v0.2.2 起，原来的 Dashboard / Tasks / Projects / Statistics 四页合并为此一页，导航从 7 项减到 4 项：Gantt / Logs / Manage / Calendar）

- **Overall**：Projects（项目数）与 Total tasks（任务总数 + 叶子数）两张卡片占满一行；下面 Today / Upcoming (7 days) / Overdue 三列任务列表。
- **Projects**：卡片网格，含新建、内联改名、删除、进度条与完成度、`N tasks · N completed · N open`、以及「Open in Gantt」。
- **Tasks**：六张状态计数卡占满一行（In progress / Completed / Paused / Coming soon / Delayed / To-do）——其中「即将逾期」折进 Delayed 卡的副标（`past due · N due soon`），不单独占卡；下面是状态下拉筛选 + 八列表格（WBS / 任务 / 项目 / 开始 / 结束 / 进度 / 状态 / 优先级），点行开任务详情。

### 12. 设置（语言 / 主题）
- 侧边栏第 5 项。语言与主题都是**即时生效**，没有保存按钮。
- **语言**：English（默认）/ 中文 / Français。语言名用本语言自称，切换器本身不需要被翻译。
  - 226 条文案 × 3 语言，覆盖按钮、标题、**悬浮提示（`title=`）**、`aria-label`、确认框文案。
  - **日期按语言重新组句**，不是逐词替换：`Aug 14 – Oct 13, 2026` / `14 août – 13 oct., 2026` / `2026年8月14日 – 10月13日`。轴刻度 `W34` / `S34` / `34周`；法语周标签省略重复月份（`14 – 20 sept.`）。
  - **单复数按语言规则**：英语 `1 log / 2 logs`；法语 0 与 1 同为单数（`0 journal / 1 journal / 2 journaux`）；中文只有一形。
  - **完整性由编译器强制**：`Dict` 类型由英文字典推导，中法字典逐 key 标注该类型，漏一条 key 则 `tsc --noEmit` 失败——而 `npm run build` 第一步就是它。不靠人工检查。
  - 切换语言会同步 `<html lang>`（影响 CJK 字体选择与读屏发音）与 `document.title`。
- **主题**：四套。差异**主要由底面明度承担**，而不是只换强调色——初版在这里做错过一次，原因与结论见第 4.11 节。

  | | 底面明度 L\* | 色温 | 强调色 |
  |---|---|---|---|
  | 石墨 Graphite（默认） | 4.4 | 中性 | 青 |
  | 余烬 Ember | 6.9 | **暖**（每个中性色都偏棕） | 橙 |
  | 板岩 Slate | **15.1**（约石墨的 3.4 倍） | 偏冷 | 蓝 |
  | 纸张 Paper | 100 | 浅色 | 蓝 |

  - **全部色值收敛为 CSS 变量**（`src/index.css` 里每套一个 `[data-theme]` 块）。切换主题只是改 `<html>` 上的一个属性，**不触发任何 React 重渲染**，也没有中间帧。
  - **语义色在三套深色里完全一致**（绿=完成、红=逾期、蓝=暂停、琥珀=进行中、紫=待办）——这是一套用户已经学会的颜色语言。**纸张另配一套**，因为深色那组当正文放在白底上对比度不足（琥珀约 1.9:1、绿约 2.5:1），而状态色在甘特左栏与 Manage 表格里正是当文字用的。
  - **主题卡展示方案本身**：左边一块用它自己配色的迷你甘特图，右边两组色板——**界面** 10 色与**语义** 6 色，**每块下面标注名字与十六进制值**。色值不另存一份，而是让色板带上 `data-theme` 后从 CSS 级联里读回来，所以色板不可能展示应用实际没用到的颜色。
- **偏好独立存储**（key `wbs-gantt.prefs`），与数据文件分离：**导入一份 JSON 不会重置语言与主题**，导出的 JSON 也不带界面偏好。
- 已停用的主题 id（如旧版的 `basalt` / `copper`）会被 `isThemeId` 挡下并回落到石墨，不会让应用进入无配色状态。

### 13. 数据持久化
- localStorage 两个 key，**刻意分开**：`wbs-gantt.v2`（项目 / 任务 / 日志）与 `wbs-gantt.prefs`（语言 / 主题）。分开的原因见上一节——数据文件要能自由导入导出，界面偏好不该跟着跑。Web 版与桌面版均本地保存。
- **新装是空白的**：`buildSeed()` 返回空数组，不附带任何演示数据（`src/lib/seed.ts`）。
- JSON 导入 / 导出。
- 「今天」每 60 秒检查一次，跨天自动刷新派生进度/状态。
- 坑：`loadData()` 优先于 `buildSeed()`，且在模块加载时执行——所以改动 seed 后必须清掉 localStorage 并**硬刷新**（HMR 不会重跑模块级代码）才能看到。

### 14. 部署与发布
- Web：`npm run deploy` 部署到 Cloudflare。
- 桌面：`npm run tauri build` 打包 NSIS 安装包；`npm run release` 一键打包+签名+发布 GitHub Release（含自动更新）。
- 版本号写在 `src-tauri/tauri.conf.json`，由 `release.mjs` 交互式询问后写入；`latest.json` 与 `.release-notes.md` 是构建产物，已 gitignore。
- 用户看到的「更新」由 GitHub Release 上的版本号驱动，**与提交次数、push 次数无关**——只有跑 `release.mjs` 那一刻才产生一次更新。
- **「待发布」占位由脚本自动替换。** `CHANGELOG.md` 与 `FEATURES.md` 里同时含「待发布」和本次版本号的行，会被 `release.mjs` 的第 5 步换成本地日期。**只改工作区，替换后仍需手工提交**；版本号对不上（比如改了号却没写变更记录）只提示、不中断发布。此前这是手工步骤，连续两个版本忘记过。

---

## 四、需求原文摘录

### 4.1 甘特图画布与层级排版
> 按任务关系（父子关系）给任务不同的宽度以及任务名称字体不同大小和粗细……第一、二级之间 ui 的数值区别应该大于第二、三级……第四级及更高沿用第三级……缩进保留。

> 甘特图一个连续画布，两个重叠层：日期网格作为全宽底层，任务标签作为半透明（30% 透明度）毛玻璃顶层在左侧。

> 一级到二级差 12px，二级到三级差 4px。

### 4.2 时间轴
> 画布大小是 gantt 功能的整块区域……定值……实时计算。

> 默认把当前日期放在画布未被任务栏遮盖部分的……（中央改成）距离任务栏右侧一定间隔。

### 4.3 长期目标
> 把格子全部涂满颜色，然后向右逐渐减淡……30%。

### 4.4 进度与状态
> 取消长期任务的进度这一信息，因为根本无法计算。

> 任务的 progress 不再是自动按已经过去的日期与总日期计算，而是只有在当日填写任务日志之后才开始累计进度。

> 所有任务新建时进度都默认为 0。

> 非严格无逾期那就是天数走完、进度条 100 就是完成；严格任务除了每日任务日志自动累计进度（比如任务定了八天，那一天如果写了日志就应该涨 100/8）之外还能手动调进度，依旧是进度条满算完成。

> 新建任务时不再自主选择任务状态，而是自动按起始日期和当前日期计算该任务应该是什么状态。

> 取消非严格任务的逾期状态。

> 若新建任务时同时检测到勾选严格模式，且任务的终止日期早于当前日期，则增加选项来选择将该任务设为逾期 / 已完成。

### 4.5 任务日志
> 1. 对于每一个正在进行中（即 in progress，包括逾期 delayed）的任务都可以独立地点进去写任务日志。我想在鼠标悬停时出现的那几个图标里，以及任务详情页面都各放一个编写入口。

> 2. 在任务建立时新建一个可以勾选的项，主要是问用户是否要为这个任务启用一个较为严格的进度计算机制……该选项仅对阶段任务（phase）有效，如果在新建任务时选择了长期（long-term），则该选项的选项框和文本即刻变灰并不可勾选。

> 3. 基于任务日志的进度计算方法：在编辑日志界面下方添加一个默认折叠的项用于编辑当日工作后任务应达到的进度。如果不对该项进行编辑则默认将进度按原有进度计算逻辑（即已过日期/总日期）增加任务进度；当日未填写则不累计进度。

> 4. 长期任务的任务日志编写界面不含 3 中所提到的折叠项。

> 5. 在任务详情界面增加（当前任务的）历史日志查看入口。

> 6. 最左侧新增任务日志系统，并把它放在甘特图（gantt）下方。在该系统中可对任务日志进行统一管理。默认视图为以日为单位，在此视图下单日所有任务的日志被归纳到一起；除此之外还可以按任务筛选，筛选逻辑为 project-第一级 task-第二级 task，以此类推，类似 windows 任务管理器对文件的管理逻辑。

> 7. 编辑任务日志时换行行为将被视作新起一项，将在行首自动加类似「·」的标识。换行 + tab 缩进将被视为为该项添加子项，「·」标识依旧会添加，并跟随缩进。

### 4.6 暂停 / 恢复
> 当任务设为 paused 后日期条变灰，同时日期条从暂停日期截断。记录任务剩余完成天数 n，在任务恢复的时候以该日为基准自动将结束日期定为恢复日期 + n，同时日期条颜色恢复。

> 一个任务经历暂停-恢复之后日期显示应该是两端日期条。

> 任务详情界面里，任务处于暂停时，将起始和终止日期那里改为类似如下文案：「已暂停。任务起始日期：；暂停日期：；已暂停 n 日」，并在下面增加「恢复」按钮。当任务从暂停状态恢复后……将终止日期改为推迟后的终止日期，并且该项名称加括号说明是暂停后的终止日期。起始日期和终止日期之间添加折叠项，展开将按时间顺序依次展示（可能为多次）暂停日期和（可能为多次）恢复日期。

### 4.7 部署与发布
> 更改我的项目配置以使其能在 cloudflare 上部署。

> 用 tauri……一次性做到打包成 exe 并添加自动获取更新功能。

### 4.8 页面合并（Manage）
> 以dashboard为基准。首先是总览。先是projects和total tasks展示对应数量，格式和原overall差不多，只不过这两项占满同一行；随后下面today、upcoming和overdue不变。下面开始分项管理。第一项是projects，直接把原Projects的东西搬进来，包含项目完成度及项目的CRUD。第二项是tasks，首先格式和原dashboard的In Progress那栏相同，分为进行中、已完成、暂停、即将逾期、逾期、和待办6项，同样占满一行。下面是task的枚举，照搬原tasks就行，记得依旧是照搬，该有的信息要有，要能筛选任务，但是最后那个back to gantt不要。

> 那在保留按到期日计算的即将逾期，再加一个coming soon吧 就是七日内开始的任务。如果排版困难的话考虑把即将逾期以一个更小的字体或者其它表示附带信息的方式塞到逾期里。

> 状态卡改成六张吧 那个due soon按我先前告诉你的方法弄。

> 叫management得了 或者以manage为基础起个名

### 4.9 时间轴范围
> 日期最早能显示到当前日期-30天，最晚能显示到最后一个非长期任务截止日期+10日，且最少日期应显示当前日期±30天，即60天。

> 我其实可以设置一下，你计算一下除了day其它日期单位至少要在当前日期±多少个日期单位才能铺满，然后在这个数上+5即可

> 固定范围，但去掉翻页按钮

> 跟随筛选，接受弹回，只要显示的日期信息是正常且正确的就可以

### 4.10 日历
> 列开始/截止两列。

> 我能看出来右上角的方格用来表示已填的义务，中间的clip的颜色用来表示状态，但是这会早成歧义。我的建议是既然任务前面已经有图标说明是在该日截止/开始，那完全不需要再用颜色来表示任务是进行中、已完成还是什么。依旧是什么颜色都不给，写了日志就把名字划掉，然后在右上角统计。

> 如果是严格任务则字体用黄色

**一次被推翻的设计（记录原因）**：最初方案是「像往水池里灌水一样填充格子，用水位高度表示当日日志完成率」。实测后被否决：

> 由于一天义务大概也就两个左右 所以要么是0 要么是50要么是100 真的很丑 而它不是一个说要通过增加30 80的天来解决的，这种设计碰到低义务数量的天的时候就是丑，我觉得我们应该想出一个新的方案

原因值得记下来：**用连续通道（高度）去编码一个离散且分母极小的量**，只能画出三个台阶，等于声称了数据不具备的分辨率。改成离散的义务方块后问题消失。这条经验对以后任何"按比例显示"的设计都适用。

### 4.11 设置：语言与主题
> 左侧导航栏增加"设置"，目前计划先内置语言和主题功能。对于语言，目前设想是设置英语（已经存在，默认语言），中文和法文。这里我需要你去对里面的文本、甚至悬浮时出现的提示都进行翻译。杜绝机翻，表意清除无歧义，我相信你的能力。对于主题，目前设想是提供几种可选的颜色搭配。你自己来选，并且为其命名，要求风格统一。

初版主题做出来后被退回：

> 这个你先别着急 我想问你主题设计设计了个啥 除了第四个改了一点进度条颜色之外其他的变化在哪 别的不说第四个也不是我想要的效果 我说的设计主题是整个软件的配色方案发生改变。重新设计之后，设置里选取主题的ui也要改变，最起码你得展示这个配色方案都有哪些颜色吧

要求先给结果再审阅，不要再问方向：

> 你就没做出来效果 先让我看到你做出来的效果再问我

**失败原因与结论（值得记下来）**：初版四套声称在「表面明度、对比度、色温、强调色」四个维度上区隔，实际却把差异**全压在了色相和强调色**上——三套深色的底面明度只差 **2.5 个 L\***，肉眼就是同一个黑。当时的分析里其实已经写下「只换强调色会读成一套主题配四个按钮」，仍然掉进去了。

重做的原则：**底面明度必须是主变量，不能是微调对象。** 现在四套的 L\* 是 4.4 / 6.9 / 15.1 / 100，板岩的亮度是石墨的三倍多；余烬与石墨明度接近，但**整套中性色偏暖**（边框、文字、底色全是棕的），靠色温而不是明度区分。判断差异够不够，要**把四套截图并排比对**，或者直接量像素值——单看一张截图会误判（这一轮里对缩略截图误读了三次，每次都靠量数据纠正）。

---

## 五、已知待办 / 可选改进

- 暂停/恢复后的多段任务条目前为静态展示，未挂拖拽/缩放（多段拖拽逻辑待补）。
- 自动进度/状态刷新依赖 60 秒定时器，非精确到午夜瞬间。
- **日志与详情面板的选中态不一致**（刻意搁置）：点任务条选中的是任务，点日期选中的是天，于是任务详情面板与日期详情面板可能同时挂在右侧（各 320px）。日详情面板本身已能在只有甘特图的情况下独立工作。
- **`ProjectDetailPanel` 与 Manage 页的项目卡片信息重叠**（都展示进度与计数），但它是目前唯一能读到项目描述的地方，暂留。
- **Logs 页左栏的项目→任务筛选树**与甘特图左栏的树结构重复。
- **改了 `tailwind.config.js` 之后必须重启 dev server。** Vite 的 HMR 会把 `src/**` 的改动应用上去（包括 `index.css` 里手写的 `[data-theme]` 变量块），但**不会重新生成 Tailwind 的工具类**。结果是变量是新的、而 `.bg-bg` / `.bg-panel` / `.text-fg` 这些还是旧配色写死的 hex，表现为「切主题只有滚轮条变色，其它一概不动」。运行时切主题本身**永远不需要重启**——只有改配色定义（这次是把 hex 换成 `var()`）才需要。排查方法：devtools 里看 `.bg-bg` 编译成了 `rgb(var(--c-bg) / …)` 还是 `rgb(10 13 16 / …)`。
- **原生日期选择器的显示格式跟随系统 / 浏览器语言，不跟随应用语言。** `<input type="date">` 的输入格式由浏览器决定，改页面 `lang` 影响不了它。值为 `yyyy-MM-dd`，无歧义。
- **自动写入的日志正文用的是写入当时的界面语言。** 目前只有一处：把已逾期的严格任务标记为已完成时写入的「已标记为完成 / Marquée comme terminée」。之后切换语言，历史里会留下另一种语言的条目——这是**刻意**的，它和用户自己敲的日志正文同类（都是用户内容），改语言时不该被追溯重译。
- **三套深色主题仍共用同一组语义色。** 这是刻意的（保住「绿=完成、红=逾期」这套已学会的语言），代价是各套的主题感只能靠底面与强调色承担。若将来觉得差异仍不够，下一步可以给每套单独调一组语义色阶——**保持色相家族不变**，只调明度与饱和度使它们与各自底面更协调。

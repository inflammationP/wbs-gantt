# 更新日志

WBS · Gantt 每个已发布版本的变更记录。还没发版的改动，直接按下一个版本号新起一节、**日期就写当天**。**不写「待发布」占位 —— 写完就是终稿，发布时不再回头改这一节。**

`release.mjs` 第 5 步只在某一行确实还写着「待发布」时才把它换成本地日期。那是留给「写完隔了几天才发版、日期忘了核对」的兜底，正常路径下它一行都不动。一个版本号那一节本该只被写一次：写完、提交、发版，之后不再碰。

**版本号同理，而且更要紧**：第 1 步写的是 `src-tauri/tauri.conf.json`，所以写变更记录那一节时顺手把版本号也改掉，发布脚本才会什么都不用写、工作区发布完就是干净的。脚本只在版本号**没变**时才动它，动的时候会提醒你补一次提交 —— 那是没赶上正常路径，不是常规流程。

版本号取自 `src-tauri/tauri.conf.json`，每个版本对应一个 git tag 和一次 GitHub Release。桌面版通过 Release 里的 `latest.json` 自动更新。

---

## v0.5.0（2026-09-18）

### 改进

- **「非严格任务」改成「简化任务」：进度不再由日历推，改成你每天点一下。** 原来非严格任务的进度就是「已过天数 ÷ 总天数」—— 你什么都不做它也会涨，窗口走完自动算完成，热力图还把过去每一天都记成完成。现在它数的是**你点过的天**：每天在任务行上点一下方框表示「今天我做了」，那天才计入进度，没点的一天一分不进。进度 = 已确认天数 ÷ 可确认天数。
  - **暂停过的任务仍然能走到 100%。** `resumeTask` 的顺延会把结束日期往后推，窗口跟着变长；那几天若还算进分母，就等于凭空多出几天谁也点不了的日子。所以分母只数**没被暂停**的天。严格任务那条老的「已写天数 ÷ 总天数」兜底也一起改了。
  - **漏点的那天可以补。** 打开日历点进那一天，勾选框还在，点一下就是了。未来的日子灰掉，不能提前点。
  - **没有任何地方能填数字。** 勾选框能取消勾选（和日志能删一样），但进度永远是你点出来的，不是填出来的。也因此**进度可以往回走** —— 取消某天的勾选，那天的份额就退回去。这与严格任务正好相反（那边不能往回填），是这两种任务最大的区别。
  - 分子裁到「查询日」为止：`dayTasks.ts` 的 `progressBefore` 靠传入「前一天」来问「那天开工时完成了没有」，不裁就会把今天点的那格算进昨天。

- **简化任务现在会逾期。** 原先非严格任务没有逾期态，因为它的窗口走完**就是**它的完成 —— 日历替它把活干完了，它不可能迟到。现在它只在你点过确认时才前进，所以会像别的任务一样跑完窗口，`delayed` 那条 `strictProgress` 前提随之拿掉。进度 ≥100% 仍然先判完成，确认满的不会误判成逾期。

- **新建任务默认是严格任务。** 表单里那个勾选框反过来写：「取消日志限制（不推荐）」，默认不勾，挪到**表单最后**并用一条分隔线隔开 —— 它是退出默认做法的一条路，不是又一个要填的字段。
  - **勾它先弹窗**（`components/LogOptOutDialog.tsx`）：三段讲清代价（失去历史日志、日志页不再管理、改用什么算法、监督力度几乎为 0），按钮「返回」（左、带强调色、拿焦点）与「仍要取消」（右、朴素）。**取消勾选不需要这道手续** —— 那个方向本来就是推荐的方向。
  - **长期任务不显示这个框**（没有进度，也就没有日志可言）。这与第 4 节里「长期任务时勾选框变灰」的原始需求不同：那条写在勾选框语义还是「开启严格进度」的时候，语义反过来之后，一个灰掉的「取消日志限制」只会误导。
  - 「开始待办」弹窗同一套（`TaskDialog` 与 `StartTodoDialog` 共用那个弹窗组件）。顺带修了一处：以前把类型切到长期再切回来会静默地把 `strictProgress` 清成 false，现在不会了。

- **界面不再管有日志义务的任务叫「严格任务」。** 日期详情里原本分成「严格任务」「其他任务」两份清单，现在合成**一份「任务清单」**：欠日志的任务在名字前面带一个琥珀色的 `*`，**清单标题底下**有一行小字说明这个记号（当天没有带记号的任务时这行不出现 —— 没有记号可解释还常驻一行说明，就是那盏「恒亮的绿灯」）。拆掉那个分组，是因为它逼着每个任务二选一取名，而真正要说的只有一件事 —— 它欠不欠日志，用一个记号说比用一个标题说准确。日环还在数它们，「欠着的排在最上面」没丢。
  - **同一批措辞改动**：日面板头部的「严格日志」卡改成「日志」；日环的「没有严格任务排期 / 严格任务已全部记录」改成「当天没有日志义务 / 该写的都写了」；日历格子的提示改成「没有日志义务 / 已记录 N/M 项日志」；日历里程碑的后缀从「· 严格」改成 `*`；详情面板的进度模式从「严格（按日志推进）」改成「按日志推进」。
  - **代码里的名字没动**（`strictProgress`、`isStrictLeaf`、`day.strictLogs` 这些 key 名）—— 那是实现的名字，不是给人看的名字。**入门引导也没动**：那一篇整章都在对比严格与非严格，对比本身就是教学内容，改了要重写三语讲解。
  - `daySummary` 因此多了一个 `all` 字段（合并后的那一份清单）；`strict` / `nonStrict` 留着给还在用它们的计数。

- **那份清单改成任务树。** 父任务后面紧跟它自己的子任务，按 `depth` 缩进（`INDENT = 12`，比甘特图的 16 小 —— 日面板最窄只有 320px）。**同级里带 `*` 的排前面**，所以「欠着的排最上面」没丢，只是从「整份清单按紧急度排序」变成了「每组内部排序」。
  - **排序本来就已经大致是树**：`wbs` 是层级编号，同状态同项目时它自然把父子排对。真正新的是缩进（`DayRow.depth` 一直算着，只是没人用过）和同级优先规则。当天没排期的父任务不会把子任务卡住 —— 那一条自己当根。
  - **当天没有带 `*` 的任务时，标题底下那行说明不出现**：没有记号可解释还常驻一行说明，就是那盏「恒亮的绿灯」。
  - `check-day-obligations.mjs` 补了三条断言盯着这个顺序（含一条专门证明「同级严格优先」而不是「编号碰巧排对」：让不带义务的兄弟拿到更小的 `wbs`），实测去掉那半句规则就会失败。

- **同一条任务同一天再写日志，不再新建第二条，而是接到当天那条后面。** 以前重新进入编辑会留下两条日期相同的记录：它们在历史里读起来像两天的工作、在任何按天计数的地方被数两次，而且「哪一条是今天的进度」得看它们的先后顺序。现在内容换行追加，进度用新填的那个（同一天的后来声明算数）；新填的进度为空时不覆盖当天已有的数。
  - 合并做在 `addLog` 里而不是弹窗里 —— 那是日志唯一的入口，所以「逾期的严格任务标为已完成」那条自动写入也一并合并了。
  - 顺带消掉一处隐患：`taskProgress` 按日期排序后取最新一条，同一天有两条时谁赢取决于数组顺序。一天一条之后这件事不再存在。
  - **两次填写用一条时间分隔线分开**（`— 14:32 —`，渲染成一条带时间的细横线）。它写在正文里而不是另存字段：正文是用户的文字，分隔线也该是 —— 可以编辑、删掉、手打。日志页的预览会跳过它，不占那两条预览名额。两处内联渲染日志的地方（日日志弹窗、任务详情的 History）原来各有一份一样的标记，现在合成 `components/LogLines.tsx` 一份。

- **老日志的进度基线改了。** 基线原先是「那条日志**当天开始时**任务的进度」，这对 v0.4.1 之前写下的日志不成立：那些日志全都带着空进度，任务的数字是旧规则（已写天数 ÷ 可确认天数）推出来的。结果是在弹窗里编辑它们时，底下写着「目前进度 0%」而任务显示着 17%；而且在任何一条老日志上填一个数，那个数会**直接变成整个任务的进度** —— 能掉到 0%，也能往上跳。
  - 现在：**整条任务一条明确进度都没有时，基线改用任务当前显示的进度**（`LogDialog` 的 `base`）。标签跟着变真，低于当前值一律被「不能往回填」挡下。
  - **有明确进度的任务完全不受影响**：`fr-u4` 那种逐日 8 → 12 → 20 → … 的按天基线原样，补写旧日期的值照旧自由，不会被当前值卡住。

### 修复

- **热力图不再被非严格任务糊成一片纯色。** `heatmap.ts` 原来给非严格任务的规则是「那天已经过去就算完成」，于是「完成数」等于「当天在飞的件数」，一整年的格子糊成同一个颜色 —— 这条一直记在「已知结果」里。现在只有你点过确认的那天算完成。
  - **副作用说清楚：** 非严格任务为主的板子，热力图会从「一片纯色」变成「几乎全空」。这是诚实的，但看着像坏了。

### 说明

- **文案里的强调记号。** 确认弹窗的文案由用户定稿，存进字典时保留他写的记号：`*加粗*`、`/放大/`，斜杠个数即档位，封顶 120%。用记号而不是 `useTRich` 的占位符，是为了他以后还能用同一套记号直接改文案。解析器 `src/lib/emphasis.ts` 返回数据结构而不是 JSX，所以有独立 check，重点盯着两类不响的错：**落单的记号必须按字面输出**（否则后半段会无故变粗或连同消失），以及正文里那个**全角 `／`**（「暂时忽略／将确认补上」）不许被当成记号。
- **弹窗里提到了一条还不存在的功能**：「如果你有某日忘了点按钮，则会在以后的日报里对你进行提醒」。用户明确要求先留着文案、功能后续再补 —— 这是他的决定，不是漏项。
- **`Task` 新增 `confirmedDays: string[]`**（简化任务已确认的日期）。存日期数组而不是带数字的记录，也不复用 `TaskLog` —— 用户要求它不产生历史日志、不被日志页统计。它和日志一样能穿过待办：任务转成待办再转回来，点过的天还在。
- **`autoProgress` 与那条「暂停期间冻结在暂停日」一起删掉了。** 后者不但没用了，还会遮掉恢复之后点的确认。`isPausedOnDay` 从 `dayTasks.ts` 搬进 `progress.ts`：新分母要用它，而 `dayTasks` 已经依赖 `progress`，反向导入会成环。
- **`daySummary` 多了一个 `today` 参数**，用来判断某一天还能不能点。
- **`FEATURES.md` 第 4、5、7 节与热力图那条已知结果**跟着改了；**`README.md` 第 13 行仍写着旧规则**（「非严格任务的进度条自动随日期均匀增长，到100%后自动标记为完成」），那个文件按约定不能由我改，需要手工处理。
- **`scripts/check-simplified-progress.mjs` 与 `scripts/check-emphasis.mjs` 是新增的**；`check-heatmap.mjs` 里凡以旧规则为前提的断言全部重写（连注释里那句 “the non-strict child is done on every day that has passed” 一起）。这两条新 check 都实测过在改动前的代码上会失败，不是摆设。

## v0.4.1（2026-09-17）

### 改进

- **日志弹窗里的进度改成两个联动的格子，且必填。** 原来是一个折叠起来的「设置目标进度（可选）」单项，不填就按「已写天数 ÷ 窗口天数」自动累计 —— 于是「今天没干」和「干了但忘了填」在数据上分不出来，进度自己往前跑。现在常显两格：**「进度增加（%）」**与**「进度累积至（%）」**，填哪个都行，另一个按**当天开始时的进度**自动算出（这个值以小字显示在两格下方）。累计进度钳在 0–100。两格全空时提交，**两格一起标红**并提示「至少填一个」；**进度不能往回填** —— 累积至低于当天开始时的进度时提交，同样两格标红并报错。两种情况下都不保存。
  - **要的是「今天没干」一步写完。** 只有一个绝对格时，写 0 得先知道现在的值是多少；有了增量格，说「今天没进展」就是往增量格里填个 0。往绝对格里填 0 是另一回事 —— 那是把进度清零，会被当成往回填挡下来。
  - **编辑旧日志时两格都预填好**，什么都不改直接保存是彻底的 no-op：原样写回，不给这行新添一个它本来没有的进度值。**「不能往回填」只查用户真正填过的值**，所以旧数据里那些回落过的记录仍然能原样保存出去。
  - 增量格用 `type="text" inputMode="decimal"` 而不是 `type="number"`：number 框里打一个 `-` 会被报成空串 —— 那样「打了一半」和「没填」就分不出来，报错会串成「至少填一个」而不是「不能往回填」。
  - 顺带补了一条：日期被清空时不再让保存通过（写进去的 `date: ''` 排在所有真实日期之前，会落进每一天的查询）。

- **今日页任务行的「写日志」按钮换位置、换图标。** 原来绝对定位在行尾最右侧，会被 `No log` / `Overdue` 徽章挡在后面、看着像徽章的一部分；现在跟在任务名后面固定 8px 处，徽章仍靠右（`ml-auto`）。任务名因此不再 `flex-1` 撑满 —— 否则「跟在名字后面」会变成「跟在名字占掉的那片空白后面」，位置随名字长短漂移。图标从 `NotebookPen` 换成 `NotebookText`：带铅笔的样子和「编辑这条任务」的铅笔撞车。右键菜单与任务详情面板里同一个动作也一并换了 —— 同一个动作不该在两处长得不一样。
  - **行的点击区域没有变小。** 整行仍然是展开/收起，点击落在行内任何地方（含徽章、含中间空白）都算，只有写日志那个按钮 `stopPropagation`。

### 修复

- **一篇不填进度的日志不再抹掉之前明确写下的值。** `taskProgress` 原来只读最新一行日志，那一行没填目标就退回「已写天数 ÷ 窗口天数」。于是「10 号写下 100%、12 号随手记一篇不带进度的」会把任务从已完成退回未完成，并从此天天进欠日志名单。现在从最新**往回找第一篇填了进度的日志**，跳过没填的那些。

### 说明

- **变更记录不再有「待发布」占位，写完即终稿。** 此前一节会被改两次：先写「待发布」，发布时由 `release.mjs` 第 5 步换成日期，于是 v0.3.4 与 v0.4.0 各留下一个只为收尾而存在的提交（`86f6f85 file update following v0.3.4`、`9e9f85e 0.4.0`）。现在日期在写的时候就写死，脚本那一步降级成兜底 —— 只替换确实还写着「待发布」的行，正常路径下什么都不动。版本号同理：写这一节时顺手把 `src-tauri/tauri.conf.json` 也改了，脚本第 1 步就无事可做，发布完工作区是干净的；它只在版本号没变时才写，写的时候会提醒补一次提交。规则写在 `CHANGELOG.md` 开头与 `FEATURES.md` 第 17 节。

- **那条自动累计的兜底保留着，只剩旧数据走得到。** 删掉的话，全部日志都没填过进度的严格任务会归零；更糟的是写满窗口因而被算成 100% 的任务会突然变成 0%，并从此天天欠日志（环的分母整个跳一次）。保留它，旧数据的数字一个不变，界面上则再也造不出没有进度的日志。入门引导里那段讲解（「一条都没填就按已写天数 ÷ 窗口天数累计」）因此仍然成立，三语都没动。
- **不动「非严格任务」那一类**（进度随日期自动涨、到期自动完成）：`autoProgress`、`strictProgress`、新建任务窗口里的勾选框全部原样。
- **`strictLogObligations` 的判定没变**，但 `check-day-obligations.mjs` 补了两条断言 —— 原来的 fixture 凡涉及进度都显式填了 target，对这次改动完全没有感知。新断言在改动前的代码上会失败。
- **`FEATURES.md` 第 4 节与第 7 节**跟着改了：进度规则补上「往回找」，日志编辑器一条改写成两格联动 + 必填。

## v0.4.0（2026-09-17）

### 新增

- **导航栏新增「今日」页**（侧栏第 2 项）。左右两栏 = 今日 / 明日；点哪一栏哪一栏就聚焦 —— 变宽（flex-grow 1 → 1.5）并恢复正常亮度，另一栏收窄、压暗到 45%。默认聚焦今日，点栏内任意位置即切换。
  - **每一栏渲染的是完整的一天，不是一张清单。** 顶上是严格日志覆盖环 + 三张数字卡（严格日志 / 任务 / 临时事务，格式与 Manage 页开头的总览卡相同），下面左侧当天任务、右侧临时事务，底部「查看当天日志」。所以**从今日页就能写日志**：严格任务行悬停出笔，点开当日日志弹窗。
  - **与日期详情面板共用同一个组件**（`components/DayBoard.tsx`），差别只有 `layout`：320px 的面板把任务与事务上下叠放、环旁边换回三行文字（三张卡在 320px 下每张只剩约 80px）。两个界面因此不可能对同一天得出不同答案。
  - **事务区块的宽度有上下界**（34%，240–320px）。无下界时，非聚焦那一栏会把事务栏压到约 196px，标题被挤成一行一个词；无上界时它会在宽屏上撑开、把任务那侧挤得参差。顺带把 `↺` 顺延角标从标题那一行移到了副行 —— 和标题抢同一行宽度，输的总是标题。

- **新增一类实体：临时事务（Chore / Menue tâche）** —— 短期待处理的小事，和甘特图解耦。
  - **独立集合 `chores[]`，不是给 `Task` 加标记。** Gantt 侧的树、进度汇总、时间轴范围、项目进度这些代码一条都不读它，所以「解耦」是结构性的，不靠每个地方记得排除。对照：现有的「待办」（`Task.isTodo`）走的是加标记那条路，`isTodo` 散在 15 个文件、69 处。
  - 字段 `id / title / note / date / done / completedDate / createdAt / updatedAt`。
  - **`date` 是「最初定在哪天」，永不改写。** 顺延靠读（`date <= 今天 && 未完成`），不靠写。所以一条拖了四天的事务会一直告诉你拖了四天（`↺ 4 days ago`），而它原本该做的那天也仍然读得出来 —— 把日期改写成今天的做法会让「昨天到底该做什么」永远无法回答。
  - **热力图按「勾掉的那天」计，不是「定下的那天」**（存 `completedDate`）。否则周二补写周一的事务会把周一追溯点亮，而周一确实是在没做完的状态下过去的。
  - 新建、勾选、编辑都只在今日页做（悬停出铅笔，弹窗改标题 / 备注 / 今天↔明天）。**日期详情面板里只展示、不可操作** —— 第二个能勾事务的地方，就是第二个会和这里对不上的地方。

### 改进

- **逾期的严格任务纳入欠日志清单。** `strictLogObligations` 里「窗口是否还罩着今天」（`activeOnDay`）换成了「是否已经到期」（`hasComeDue`，只看开始日期）。严格任务窗口走完还没做完，**从那天起每天都欠一篇日志，直到做完为止**。
  - 此前它恰好在窗口结束那天从清单里消失 —— 而那正是它变成整块板上最紧急的事的时候。
  - **顺带消掉了一处界面自相矛盾**：逾期严格任务一直挂着 `No log` 徽章却从不计入环，于是出现「环说缺 2 个、列表上有 4 个徽章」。现在两者由同一个判定产生（今天：`3/7 = 43%`，环说缺 4，列表上正好 4 个徽章）。
  - **代价：日历的格子跟着变。** 任务滑掉之后每一天都会继续标它欠着，一块拖延的板上日历会红成一片。这是这条规则的必然结果，不是缺陷；若觉得吵，可以只让「今天」这么做、日历仍按窗口算。

- **任务列表里删除线的含义按类型分开。** 严格任务写了今天的日志就划线（与环的分子逐条对应）；普通任务仍是真完成了才划线。此前一律按 `status === 'completed'`，于是「划了线的没进环、进了环的没划线」—— 一个 35% 但今天写了日志的严格任务看起来像没动过，而一条窗口自动走完的普通任务看起来像被处理过了。

- **Manage 页那张数字卡抽成了共用组件**（`components/ui.tsx` 的 `Stat`），今日页头部用同一个。不是排版问题 —— 两处各写一份，等于「一个数字长什么样」有了两个答案。

### 修复

- **`progressBefore` 没有真正裁到「前一天」。** 它把**今天的**日志一并交给 `taskProgress`，而后者对带目标值的严格任务根本不读 `onDate`（只取数组里最后一条日志）。结果是：写好今天这篇完成日志，会把这条任务从今天的名单里踢出去，环的分母随之变小 —— 3/5 悄悄变成 3/4，看起来像进步。代码注释里写的正是要防这件事。
  - 由新增的 `scripts/check-day-obligations.mjs` 抓到。这条判定同时驱动日环、日历格子与任务面板的待填列表，改动会一次移动三处，而且是静默的，所以它现在有了自己的检查文件（另外两个是 `check-heatmap.mjs`、`check-chores.mjs`）。

### 说明

- **`seed.ts` 保持空壳。** 演示数据集在 `seed-tutorial` 分支，不在 `main`。本次唯一改动是空壳多返回一个 `chores: []`（多了第四类数据），文件里补了一行注释说明原因。
- **`release.mjs` 的发布说明支持多行了。** 原来用 `rl.question()`，该 API 读到第一个换行就返回 —— 按 Enter 是在提交而不是换行，所以发布说明一直只能写一行。现在读到空行为止（直接回车仍等于「不写」，Ctrl+D / Ctrl+Z 回车可提前结束）。这些说明原样进 GitHub Release 正文与 `latest.json`，而更新对话框按 markdown 渲染，所以现在写 bullet 会真的显示成 bullet。
- **`FEATURES.md` 新增第 12 节**「今日页与临时事务」，原第 12～17 节顺延为 13～18。
  - **因此本文件里更早的条目中出现的「第 N 节」不再对应当前编号**（例如 v0.3.4 提到的「第 17 节」现在指第 18 节）。那些是当时的编号，保留原样不改写 —— 这一条之后的引用才按新编号写。上一处顺延（v0.3.3 新增第 14 节）当时也只在那一节里记了一句，未回改更早的条目，此处沿用同一做法。

## v0.3.5（2026-09-15）

### 新增

- **任务详情栏和 Manage 页各多了一块「完成热力图」。** GitHub 贡献图那种格子：一列一周、一行一天，列顶标月份、左侧三行周几、底部一条图例。任务详情栏那块画所选任务的排期区间，往回最多 20 周（320px 面板的宽度上限）；Manage 那块画整块板，从今天往回 53 周（一年），排在总览以下、Projects 以上。
  - **「一天算完成」只有一处定义**（`src/lib/heatmap.ts`）：当天排期内的任务都做完了才算 —— 严格任务要当天有日志，非严格任务只要那天已经过去；暂停的日子不算排期。两块共用这一份，不是各画各的。
  - **两块用不同的深浅语言，因为两处的「量」不是一回事。** 任务详情栏是一个任务，它的一天只有完成/未完成两态，再加档位等于声称数据不具备的分辨率（同 `FEATURES.md` 4.10 那条被推翻的设计）；Manage 是一整块板，一天有大小，所以按**当天完成的任务数**分四档：1 / 2–3 / 4–9 / 10+。
  - 配色取自 `completed` 信号 token 的四档透明度，四套主题自动跟随，没有写死颜色。悬停每格给出日期和状态（Manage 那块是「完成 N 项任务」）。

### 改进

- **甘特图左栏那枚小圆点，从「优先级」改成了「项目」。** 左栏是多项目混排的，而这枚点该回答的是「这一条属于哪个项目」—— 身份，不是状态；项目色是这一列里唯一能提供身份的东西。**优先级并没有消失**：任务详情栏和 Manage 的表格里各有一份。
  - 点用项目自己的身份色，悬停显示「项目：<名字>」。**待办的虚线圆点不变。** 项目 id 悬空（手改过的导入数据）时这一点不画，而不是画出一个 undefined 颜色。

### 说明

- **甘特图日期条维持原样。** 曾计划把日期条的语义改成「只有当天完成才为当天填色、未完成只留彩色边框」—— 逐日格子，为的是让甘特图上的完成天数可以直接数 —— 用户在前一版方案确认时否决，**这个改动从未落地**，`gantt/TaskBar.tsx` 一行未改。
  - 该方案需要的「某天算不算完成」判定没有浪费：它成了上面两块热力图的引擎。
  - **日历相关代码未改动。** 逐日规则没有加进 `lib/dayTasks.ts`，而是单独放在 `src/lib/heatmap.ts`，只借用 `dayTasks.ts` 既有的 `activeOnDay` / `isPausedOnDay` 两个原语；`dayTasks.ts` 与 v0.3.4 逐字节相同。

## v0.3.4（2026-09-14）

### 改进

- **「我知道了」按钮取消，红色横幅改为每次检测失败都出现。** v0.3.3 里那条离线提示可以被永久关闭，现在不行了 —— 而且不论这次检测是启动时自动发起的还是用户手动点的，只要没能连上就会出现。
  - **原因是它本来就该这样。** 横幅陈述的是一个**持续成立的状态**（连不上 GitHub），而不是投递一条一次性通知。所以「关闭一次就永不再提」站不住：关掉之后状态依然成立，界面却在说谎。唯一的消除办法是让应用真能连上 GitHub，这正是它的目的；版本区块下面那块 Watt Toolkit 说明就是常备的出路。
- **启动时的自动检测也会给出回执了。** 确认已是最新版本时，在版本号那一行的右端显示「已是最新版本。」—— 此前启动时那次检测完全不可见，用户看不到任何表示它跑过的痕迹。
  - 失败的回执被去掉了（原来在版本行里另有一行灰色的「无法连接到 GitHub。」），因为红色横幅已经把这件事说得更清楚，两处重复只是噪声。
- **启动时的自动检测也改成先问再装。** v0.3.3 里只有手动检查会停下来问，启动时那次检测发现新版本会**静默下载、安装并重启**。现在两条路径一视同仁：都弹同一个对话框，显示版本号和更新说明，由用户点「下载并安装」。
  - **代价是更新装掉之前，每次启动都会问一次。** 这是刻意的取舍 —— 悄悄重启用户的应用不可接受，而更新说明应该在用户做决定**之前**看到，而不是在重启之后。
  - 顺带删掉了 `import.meta.env.PROD` 门禁：它原本是为了防止 `tauri dev` 把正式版静默装到开发树上，既然不再有静默安装，这个门禁就没有存在意义了。
- **更新对话框多了一个「7 天内不再提醒」按钮。** 点它之后七天内不再弹这个框（天数写在 store 的一个常量里，改常量会连文案一起改）。
  - **它只静默对话框，不静默事实。** 检测照常进行，版本号右侧照常显示「有可用更新。」—— 静默提示绝不能等于把「有新版本」这件事藏起来，那也是手动点检查更新时不会被「点了没反应」困住的保证。
- **「自动检测」与「手动检测」现在字面上就是同一个调用。** 此前 `runUpdateCheck` 带一个 `mode: 'auto' | 'manual'` 参数，仅剩的区别是 30 天提醒只在自动路径触发。该参数已整个删除。
  - 理由是那个提醒陈述的是**锚点**的事实（「距上次连上 GitHub 已超过一个月」），和谁发起的检测无关 —— 所以它现在在**任何**失败的检测之后都会评估，包括手动点的那次。节流保证它不会连着打扰：同一次会话里点第二次不会再弹。

### 修复

- **`FEATURES.md` 第 17 节写错了版本号的注入方式。** 它写着前端通过 Vite `define` 拿到 `__APP_VERSION__`，实际实现早就改成了 `import.meta.env.VITE_APP_VERSION` —— 因为 `define` 在 dev 下不生效（Vite 的 define 插件对客户端模块非构建时直接返回），照原方案会在开发环境渲染设置页时抛 `ReferenceError`。文档现在与代码一致。

## v0.3.3（2026-09-14）

### 修复

- **自动更新此前从未真正生效过，而且有两个独立的原因。** v0.1.0 到 v0.3.2 的 11 个版本装的全是一个不会工作的更新器，失败还是完全静默的。
  - **capability 里没给更新器授权。** `src-tauri/capabilities/default.json` 只授予了 `core:default`，而 Tauri v2 的插件命令默认全部拒绝。于是每次启动 `plugin:updater|check` 都被 ACL 打回，异常被 `updater.ts` 的 `catch` 降级成一句 `console.warn` 就吞掉了 —— 用户侧表现就是「什么都没发生」。现在补上了 `updater:default` 与 `process:default`。
  - **安装包的下载地址一直是 404。** `release.mjs` 用本地文件名拼 URL（`WBS%20Gantt_…`），而 GitHub 上传时会把文件名里的空格改写成点（实际存成 `WBS.Gantt_…`）。逐个核对过 v0.1.0 / v0.2.0 / v0.3.0 / v0.3.1 / v0.3.2，**每一个都返回 404**。现在改成发布后回读 GitHub 实际使用的资源名再拼 URL，从根上不可能再猜错。
  - **已经装了 v0.3.2 及更早版本的用户无法自动升级到这一版。** ACL 编译在 exe 里，旧二进制改不了，他们需要手动下载安装一次；从这一版往后装的用户才会真正自动更新。这一条必须写进发布说明。
- **发布流程改为「草稿 → 转正」。** 先建草稿 Release、传安装包、回读资源名、写 `latest.json`、最后才转正。此前若中途失败，会留下一个「线上可见但没有可用 manifest」的 Release，而 `releases/latest` 会一直指向它 —— 等于全体用户静默失去更新。

### 新增

- **设置页新增「版本」区块**，显示当前版本号，并提供手动「检查更新」按钮 —— 不必等到下次重启。
- **连不上 GitHub 时会明确说出来。** 版本号下方出现提示横幅，侧栏的设置图标右上角同时出现一个小红点；点「我知道了」永久关闭，此后不再出现。
- **手动检查发现新版本时先问再装。** 弹窗显示新版本号、当前版本，以及该版本的更新说明（取自 Release 里的 `latest.json`），由用户决定是否「下载并安装」。启动时的自动更新行为一个字没变，仍是静默安装并重启。
  - **更新说明的链路早就存在，只是此前没人读它**：`release.mjs` 一直在把交互输入的 notes 写进 `latest.json`。
- **超过 30 天没能连上 GitHub 时，启动会提醒一次**，措辞是推动用户去看一眼有没有修复和新功能。
  - 计时锚点是**最近一次成功联系 GitHub 的时刻**，所以「连不上」这件事本身不会重置它；新装用户会在首次运行把锚点写成当下，不会第一天就被提醒。
  - 节流在**弹窗显示时**落盘而非关闭时 —— 否则用户在弹窗开着时强杀进程，之后每次启动都会被弹。
  - 该提醒只在桌面端启用：Web 版的锚点永远不会自增，放行的话浏览器用户会每 30 天被一个与自己无关的弹窗骚扰。
- **设置页常驻一块 GitHub 说明与连接工具推荐**，附 Watt Toolkit（原 Steam++）官网链接。推荐之前核实了两件事：官网 `steampp.net` 由官方仓库 `BeyondDimension/SteamTools` 声明的 homepage 交叉确认；以及更新器走 `rustls-platform-verifier`（Windows 系统证书库），因此 Watt Toolkit 的自签根证书**会被信任** —— 它是帮忙而不是帮倒忙。若它走 webpki 内置根证书，这条建议反而会把更新搞坏。
- **新增 `tauri-plugin-opener` 依赖**，用于把外链交给系统浏览器。此前 `src/` 里没有任何打开外链的能力，而普通的 `<a href>` 在 Tauri webview 里不会跳出应用 —— 会点不动。链接一律渲染成按钮，就是为了不留下这条静默失败的路径。

### 改进

- **前端终于知道自己是哪个版本。** 此前前端没有任何版本号来源（`package.json` 停在上古的 `0.1.0`）。现在由 `vite.config.ts` 把 `src-tauri/tauri.conf.json` 的 `version` 经 `import.meta.env` 发布成 `VITE_APP_VERSION`，桌面版与 Web 版共用同一份真值，且不新增运行时依赖。唯一真值仍是 `tauri.conf.json`，`release.mjs` 也只改它。
- **更新检查加了 15 秒超时。** 此前没有超时，网络被墙时请求可能挂很久，导致「连不上」的提示出现时机不可预测。
- **开发环境不再会自动安装正式版。** `tauri dev` 与打包版共用同一个 endpoint，此前一个落后于最新 Release 的开发构建会把正式版静默装到开发树上。自动安装现在只在 `import.meta.env.PROD` 下执行。
- **`diffDays` 不再被喂完整 ISO 时刻。** `toDate` 按 `-` 切分并期望 `yyyy-MM-dd`，传入完整 ISO 时刻会静默解析成当月 1 号 —— 一个不会报错的错误日期。

### 文档

- `FEATURES.md` 新增「版本与更新」一节。
- 设计计划归档：`docs/plans/2026-09-14-update-ui.md`，索引在 `PLANS.md`。

## v0.3.2

### 新增

- **父任务上的「今日待填」提醒。** 选中任何一条有子任务的任务，详情面板会列出**今天还没写日志的子任务名字** —— 只有名字，不可点，日期和正文都不在这里。一条不欠时显示 `All strict work logged`，这才是查漏补缺的落点：确认没有漏的。
  - **只列每根分支最末端的子任务。** 严格标记在带子任务的任务上是空转的（父任务的进度是子任务的平均），所以「该写日志的」永远是叶子；中间层即使带着严格标记也不会出现在这里。
  - **和日面板的日志环是同一套判定，不是抄的一份。** 那个合取（严格叶子 ∧ 当天在窗口内 ∧ 当天没暂停 ∧ 昨天之前没到 100% ∧ 今天没写过）抽成了 `strictLogObligations`，日志环与日历格子也改走它。抄一份的话迟早会漂成「环说欠 2 条、列表只列 1 条」。
  - 其中「昨天之前没到 100%」那条意味着**已经做完的任务不算欠**，所以提醒不会去唠叨完成的工作。
- **写子任务日志可以一次填完。** 父任务的「写日志」按钮打开的还是那个日志界面，只是多一个**待填子任务下拉菜单**：填完一条不关窗，自动跳到下一条并把日期／正文／目标进度清空，按钮从「保存并继续」变回「保存」；填完最后一条才自动关闭，中途可以按「取消」退出。
  - 下拉里的选项**与上面那条提醒逐个相同** —— 同一棵树、同一套判定、同一份顺序。

### 改进

- **详情面板的「进度模式」不再对父任务说谎。** 此前它只读 `strictProgress`，于是带子任务的严格任务也显示 `Strict (log-based)` —— 而全应用其它地方（日环、日历、入门引导的提醒）都当这个标记不存在。现在有子任务时显示「由子任务汇总」，说的才是实话。这正是上一条提醒容易被误读的根源：一条空转的标记看起来像一条待填的任务。
- **写日志弹窗不再给带子任务的任务显示「设置目标进度」。** 同一个 `strictProgress`，同一个毛病：那个值只会在严格**叶子**上被读回来（`taskProgress` 只在叶子分支被调用），所以父任务上填了也是白填 —— 一个没有任何地方读的数字。现在有子任务时不显示这一栏。
- **没有待填缺口时，父任务的「写日志」退回成原来的日志按钮**，写在这条任务自己身上；叶子任务与编辑已有日志的行为一个字没变。

### 文档

- **设计计划归档的索引挪到了仓库根目录**（`docs/plans/README.md` → `PLANS.md`），和 `README.md`、`CHANGELOG.md` 并排。计划正文仍在 `docs/plans/` 下，索引里的链接已一并改成 `docs/plans/…`。顺带把「入门引导」那一份漏登的记录补上，并新增本次计划的条目。
- **`FEATURES.md` 新增第 14 节**「父任务的待填提醒与批量填写」，原第 14、15 节顺延为 15、16。

---

## v0.3.1 — 2026-09-13

### 改进

- **入门引导第 3 步：长期目标也算父任务。** 原先只认阶段任务，而新建任务的表单本来就允许拿长期目标当父任务——`parentOptions` 只排除待办，长期目标一直在下拉里。引导不接受它自己的界面允许的东西，用户照着做也打不了勾。
- **入门引导：打勾之后不再因删除而回退。** 判定原本每次渲染都从看板重新推导，而 `deleteTask` 是**级联**的（`collectDescendants` 加上它自己，连日志一起删）——删掉一个父任务会连同严格子任务和它的日志一起消失，于是第 3、4、6 步会一起退回未完成，看着像丢了进度。现在推导出即记下（`GuideStep.derived` → `guideDone`），做过就是做过，整理看板不该改写这件事。看板被清空（一个项目都不剩）时视为重来一遍，只清掉「声称看板上有东西」的那几条记录——只清这一组，因为空看板正是第 1、2 步的执行现场。
- **入门引导不再涉及任何演示数据。** 第 7 步原先会追加一块示例看板（`src/lib/sample.ts`），现已整体移除：走过的每一步都留在用户自己的看板上，第 7 步带你去看的日志、Manage 和日历读的也是它。教学一旦依赖演示数据，用户在自己的东西上学到的每一步都得在一个陌生看板上重新对位一次。
- **第 5 步的讲解补上长期目标这一例外。** 父任务结束日期由子任务决定的规则不适用于长期目标——`syncParentEnds` 直接跳过它，它没有结束日期，所以讲解原来会在这里说错。同时把「你刚加的子任务结束得更晚，所以父任务挪出去了」改成规则陈述：原说法还隐含假设了用户填的日期一定落在父任务之外。
- 第 7 步的弹窗开头不再提示例看板，改成「这四个页面读的都是你刚建起来的看板」。

---

## v0.3.0 — 2026-09-13

### 新增

- **入门引导（七步）。** 右下角浮出一张卡片，七步走完一个最小可用的看板：**选界面语言** → 建项目 → 建父任务 → 在父任务下挂一个严格子任务 → 读「父任务的结束日期是怎么来的」 → 给严格子任务写日志 → 逛一圈（日志 / Manage / 日历 / 设置）。卡片显示「第 N / 7 步」，当前步高亮、后面的压暗。关掉之后，侧栏底部的 `?` 随时重开。
  - **第 1 步先问语言**，因为界面默认是英文——不读英文的人在「Settings」面前没有别的线索。弹窗把三种语言名以**各自的语言**列出（`English / 中文 / Français`）；已经自己摸去设置换过语言的，这一步自动算完成。
  - **叫你去点哪里的步骤，用侧栏自己的说法指路**：第 1 步说「在设置里」，第 2 步说「在 Manage 里」。
  - **动作步骤全部由看板状态推导，不埋点。** 刷新不丢进度，也不可能打勾一个看板上并不存在的东西。
  - **第 5、6 步的讲解直接对着代码里的推导写**：父任务的结束日期由结束最晚的子任务决定（界面上那个灰掉的结束日期框就是它，进度是子任务的平均）；严格任务只认日志——一条没有就恒为 0%（哪怕窗口早就过去），最新一条填了目标进度就以它为准，一条都没填就按「写过日志的天数 ÷ 窗口天数」累积。
  - **不做聚光灯式引导。** 蒙层与锚点要手写（依赖表冻结）、文案要乘三（三语由编译器强制齐全）、而且锚点失效是**静默**的——改动布局后高亮框圈在空处，开发时看不出来。四套配色也一并否掉了截图方案。
  - 第 7 步会**追加**一块示例看板（不是替换，你自己建的东西都还在），日志、日历和 Manage 页因此立刻有内容可看。追加是幂等的，重复打开不会多出第二份。
  - 第 7 步以**四个页面都逛过**收尾：每点一次「带我去」记一个，卡片上显示「1 / 4」，弹窗里去过的打勾。
- **设置页。** 侧边栏新增第 5 项「设置」，内置两件事：**界面语言**与**配色主题**。两者都是即时生效，没有保存按钮——与全应用「改了就是改了」的交互一致。语言与主题存在独立的 key（`wbs-gantt.prefs`）里，**不混进数据文件**：导入一份 JSON 不会重置你的语言和主题，导出的 JSON 也不带界面偏好。
- **三语界面（English / 中文 / Français）。** 226 条文案 × 3 种语言，覆盖按钮、标题、**悬浮提示**、`aria-label`、确认框文案，以及日期格式本身。默认英文。
  - **日期是重新组句，不是逐词替换**：`Aug 14 – Oct 13, 2026` / `14 août – 13 oct., 2026` / `2026年8月14日 – 10月13日`；甘特轴刻度是 `W34` / `S34` / `34周`；法语周标签会省略重复月份（`14 – 20 sept.`）。
  - **单复数按语言规则走**：英语 `1 log / 2 logs`，而法语 0 和 1 都算单数（`0 journal / 1 journal / 2 journaux`）。此前代码里 16 处手写复数中有 5 处是「永远复数」的错误（`1 subtasks`、`1 tasks on this day`、`1 days ago`），一并消失。
  - **完整性由编译器强制**：任一种语言漏一条 key，`npm run build` 里的 `tsc --noEmit` 就不过。不依赖人工检查。
- **四套配色主题：石墨 / 板岩 / 余烬 / 纸张。** 石墨（近黑·中性）为默认，保留原有外观；余烬是暖榛黑；板岩是中灰蓝，底面明度约为石墨的 3.4 倍；纸张是浅色。四套的差异**主要由底面明度承担**（L\* 4.4 / 6.9 / 15.1 / 100），而不是只换一个强调色——这一点在初版设计里做错了，详见下方「改进」。
- **应用内确认框。** 原先 6 处 `confirm` / `alert` / `prompt` 全部换成应用内对话框。原生对话框的按钮由**操作系统**绘制、跟随系统语言，会出现「法语正文配中文按钮」；而且无法样式化。

### 改进

- **配色改为 CSS 变量驱动，一套主题就是一次属性切换。** 色值此前硬编码在三处（`tailwind.config.js`、`src/lib/ui.ts`、以及大量内联 hex），其中**强调色一个事实被抄了五遍**。现在全部收敛到 `src/index.css` 里的 CSS 变量，切换主题只是改 `<html>` 上的 `data-theme` 属性——**不触发任何 React 重渲染**，也不会有中间帧。
  - 顺带修了一个既有问题：`index.css` 从未声明 `color-scheme`，导致原生日期选择器、下拉框、复选框在近黑面板上是**浅色**的。现在随主题一起切换。
  - 主题的**语义色**（绿=完成、红=逾期、蓝=暂停、琥珀=进行中、紫=待办）在三套深色主题里保持一致——这是一套用户已经学会的颜色语言。只有浅色的纸张另配了一套，原因是深色那组当正文放在白底上对比度不足（琥珀约 1.9:1、绿约 2.5:1），而状态色在甘特左栏与 Manage 表格里正是当文字用的。
- **初版主题设计被推翻重做。** 第一版四套（石墨 / 玄武岩 / 赤铜 / 纸张）声称在「表面明度、对比度、色温、强调色」四个维度上区隔，实际却把差异全压在了色相上——三套深色的底面明度只差 **2.5 个 L\***，肉眼就是同一个黑，只有滚轮条和按钮颜色变了。重做后把**底面明度当主变量**，并保留余烬（暖榛黑）与石墨明度接近、靠**整套中性色偏暖**来区分的路子。玄武岩与赤铜两套未保留。
- **设置页的主题卡会展示方案本身。** 每套主题一张卡：左边一块用它自己配色的迷你甘特图，右边两组色板——**界面** 10 色（底色 / 面板 / 输入框 / 斑马行 / 边框 / 分隔线 / 正文 / 次要文字 / 弱化文字 / 强调色）与**语义** 6 色（待办 / 未开始 / 进行中 / 已完成 / 已暂停 / 已逾期），**每块下面写着名字和十六进制值**。色值不是另抄一份，而是让色板带上 `data-theme` 后从 CSS 级联里读回来——所以色板不可能展示一个应用实际没用到的颜色。
- **甘特图的「选中日」竖线改为中性虚线。** 它和「今天」黄线都是贯穿全高的 1px 竖线、只靠色相区分；在暖色主题下两条线几乎分不开，红色盲下完全合并。改成虚线后靠**形态**而非色相区分。
- **待办文件夹的底色改用待办色**（原为强调色）。此前文件夹的底色会随主题变成粉的、而里面的图标与名字是紫的，现在文件夹的身份始终是紫的。

---

## v0.2.3 — 2026-09-13

### 新增

- **日历格子改成「开始 / 截止」里程碑。** 每格只列当天**开始**的任务（▶）和当天**截止**的任务（⏹）。此前格子只认开始日，一个跨三周的任务只在第一天露面一次，既看不出时长也看不出重叠——而那是甘特图的主场。现在日历回答的是它独有的问题：今天有什么开始、有什么到期。只列叶子任务，因为父任务的起止是由子任务推导出来的，父子同列只会白占一行。

### 改进

- **日历的日志覆盖率从数字改成义务方块。** 右上角每个义务一个方块，写了填实、没写留空框（一条都没写时用红色描边）。义务超过 6 个时退回显示 `3/7` 这样的数字。之所以放弃原来的比例式表达：每天的义务数通常只有一两个，任何比例（高度、宽度、进度条）都只能画出 0%、50%、100% 三个台阶，等于声称了数据并不具备的分辨率。
- **日历里的任务名不再用状态色。** 状态是甘特图的事。日历里颜色只承担一个职责：**严格任务用黄色**。同时这天写了日志的任务名加删除线。两者合起来，一眼分得清「本该写、写了」「本该写、欠着」「非严格任务上的随手记录」。
- **未来日不再被当成欠账。** 严格日志覆盖率的计算对未来日期同样会得出一个非零的义务数，而未来的日志必然是空的，于是本月还没到的每一天都会被判成「一条没写」。现在未来日在日历上不做任何标记，点开详情面板显示 `Not yet due`，行上也不再出现 `No log` 警告徽章。

---

## v0.2.2 — 2026-09-13

### 新增

- **日期详情面板。** 点甘特图时间轴表头或日历上的任意一天，右侧展开当天的详情：当天排期的任务分成「严格任务」与「其他任务」两组，逐条显示进度；缺日志的标 `No log`、超期未完成的标 `Overdue`、暂停中的标 `Paused`，并可就地补写日志。底部按钮可一次读完当天的全部日志。
- **日历显示日志覆盖率。** 每个格子上多了一个 `已写/应写` 的小字，写满的绿色、有缺口的琥珀色，一眼看出哪几天该写的严格日志漏了。格子本身也可点击进入当天详情，支持键盘操作。
- **甘特图表头可点选日期。** 时间轴表头任意位置都能点击，会落到指针下的**精确日期**（周/月等粗粒度下一列跨很多天，只有指针位置能说明指的是哪一天），并打开那天的详情面板。

### 改进

- **四个页面合并为「Manage」。** 仪表盘、任务、项目、统计四页合并为一页，同一份数据不再在四处重复展示。页面分三段：总览（项目数、任务总数，以及今日 / 未来 7 天 / 逾期三个列表）→ 项目（含新建、改名、删除与「Open in Gantt」）→ 任务（六张状态计数卡 + 状态筛选 + 任务表格）。导航从 7 项减少到 4 项。
- **甘特图时间轴有了固定范围。** 最早到今天前 30 天，最晚到最后一个已排期任务截止日之后 10 天，最少覆盖今天前后各 30 天。往左滚能回看一个月的历史，不会再无限延伸；范围跟随左上角的项目筛选变化。
- **时间轴的翻页按钮改为「Back to Today」。** 去掉 ← → 两个逐页翻的按钮，改为横向滚动浏览；保留的按钮用于把视图滚回今天。

### 移除

- 删除 `todo-demo.json`（早期遗留的示例导出文件）。

---

## v0.2.1 — 2026-09-12

### 新增

- **待办（To-do）任务状态。** 一种停放「还没想好什么时候做」的任务形态：没有日期、没有优先级、没有进度，也不参与父任务的进度与状态汇总。在甘特图里以虚线框和专属颜色标识，与已排期的任务一眼可分。
- **启动待办对话框。** 把待办转成正式任务时，一次性填好开始/结束日期与优先级，不用再走一遍编辑流程。
- **待办文件夹。** 同一父任务下的待办在甘特图左栏合成一个可折叠的分组，支持勾选、批量恢复成任务、批量删除。

### 改进

- 任务描述与日志文本框中，Tab 键现在用于缩进：换行 = 新条目，行首 Tab = 子条目。

---

## v0.2.0 — 2026-09-12

### 新增

- **任务日志。** 给任务按天写工作记录，支持多级条目（换行分层、Tab 缩进）。
- **日志页。** 所有日志按日期铺成便利贴墙，每贴预览当天的任务和条目；点开可看当天全部日志，并能逐条编辑或删除。
- **严格进度。** 可以给任务开启「严格进度」：这类任务的进度不再由日期推算，只由日志累积——每写一天日志涨一格。用于那些「做没做只有日志说了算」的工作。
- **暂停 / 恢复。** 任务可以暂停，暂停期间进度冻结在暂停日；支持记录多次「暂停—恢复」的历史。
- **任务详情面板大改。** 展示日志、暂停状态、派生出的进度与状态。

### 改进

- **任务的进度与状态从「手动填写」改为实时派生。** 原先要在任务对话框里拖进度条、选状态，现在这两项都根据日期、日志和暂停情况自动算出，不再需要（也无法）手输。
- 甘特图任务条的拖拽逻辑重构，改期与改期长更稳。

---

## v0.1.3 — 2026-09-10

### 改进

- **时间轴不再随侧边面板重排。** 时间轴宽度改为按「窗口宽度 − 固定侧边栏宽度」计算，取代原先观察容器尺寸的做法。效果是打开右侧任务/项目详情面板（占 320px）时，时间轴的刻度和所有任务条位置保持不动，不会再猛地跳一下。

### 文档

- README 重写精简。

---

## v0.1.2 — 2026-09-10

### 改进

- **改变了打开应用时的时间轴定位。** 从「把当前日期放在正中」改为「当前日期停在左侧约 160px 处，时间轴主要向右延伸」。打开就看到的是一整段未来，而不是一半已经过去的日期。

---

## v0.1.1 — 2026-09-10

### 改进

- **移除任务的小时数概念。** 删掉了「预估工时 / 实际工时」两个字段、任务对话框里的对应输入框，以及统计页上那张「预估工时」卡片。
- **修正任务条的裁剪行为。** 超出日期范围的任务条现在会被裁剪到边界（左端或右端），而不是整条消失——此前右端越界的阶段任务会直接不显示，看起来像任务不存在。

---

## v0.1.0 — 2026-09-09

首个版本。

### 功能

- **WBS 任务树 + 甘特图。** 任务按父子关系组织，甘特图提供日 / 周 / 月 / 季 / 年五档缩放，任务条可直接拖拽改期和调整时长。
- **项目与任务管理。** 项目分组、颜色标识；任务的增删改查，以及右键菜单（新增子项/同级项、编辑、删除，和把任务移到顶层的「Move to top level」）。
- **任务属性。** 五种状态、四种优先级、标签，以及「阶段」与「长期目标」两种任务类型（长期目标无截止日期，在图上以开放式条形呈现）。
- **五个视图。** 仪表盘（概览指标 + 今日/近期/逾期列表）、任务列表、日历、项目、统计（按状态与按项目的分布）。
- **详情面板。** 任务详情与项目详情面板。
- **数据。** JSON 导入导出，浏览器本地持久化。新用户首次打开是空白的，不带演示数据。
- **分发。** Windows 桌面版（Tauri 打包，支持应用内自动更新）与 Web 版（Cloudflare Workers 静态部署）。
